# Local Claude Code settings (machine-specific)
.claude/settings.local.json
# Transient Claude Code agent worktrees (isolation: "worktree")
.claude/worktrees/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Node / SvelteKit
node_modules/
.svelte-kit/
frontend/build/
frontend/.vercel/

# Docs site build output (Zensical → site/; rebuilt in CI on deploy)
site/

# Env / secrets (bot API keys, etc.)
.env
*.local
.mcp.json

# OS
.DS_Store
