// SPA mode: no server-side rendering / prerendering. The spectator UI is a
// client-side app that subscribes to the backend SSE stream at runtime.
export const ssr = false;
export const prerender = false;
